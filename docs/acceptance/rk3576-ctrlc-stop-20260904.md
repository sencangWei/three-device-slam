# RK3576 interactive Ctrl+C stop acceptance — 2026-09-04

## Result

`PASS` for the approved bench/no-motion scope: D405 + STM32 acquisition, compact dual-IR H.265 recording, RGB H.265 recording, HTTP RGB preview, one real terminal Ctrl+C, orderly sealing, full decode counts, and process cleanup.

## Target and rollback

- Host: `NanoPi-M5`, Ubuntu 24.04.4 LTS, arm64, kernel 6.1.141.
- Candidate: `/home/pi/rk3576-umi-collector-candidate-20260902/three_device_slam/edge_rk3576/rsusb_capture.py`
- Installed SHA-256: `6513352a37df0e55e1ba989c52b616cf38fd70e24bb20e4a80574c8ab57953c6`
- Rollback copy: `/home/pi/rk3576-umi-collector-candidate-20260902/backups/20260904-ctrlc-signal-v10/rsusb_capture.py`
- Rollback SHA-256: `13f85a613aabd50918028994d28012b38d85ea7b7aafde2e360347c05f96c38e`
- Standard release preflight reported `MANIFEST_MISSING` because this approved deployment is a source-tree one-file candidate, not a packaged release. The exact old hash and file were preserved before replacement.

## Acceptance window

- Session: `rk3576-rsusb-20260904T064059Z-07e776d2`
- Termination: `until_signal`, `external_stop`, one terminal-generated Ctrl+C.
- Formal span: 30.127937409 s.
- RGB/left IR/right IR: 904 frames each; 30.003573 Hz each.
- Maximum arrival interval: RGB 44.925 ms, left IR 43.656 ms, right IR 43.689 ms.
- Sequence gaps, regressions, repeats, timestamp regressions, and payload-size errors: zero on all three streams.
- STM32: 12,052 packets, 399.991687 Hz; CRC, discarded-byte, sequence, IMU-validity, and encoder-validity errors all zero.
- RGB queue peak: one frame; overflow zero.
- Both IR queue peaks: one frame; overflow zero; slow writes over 33 ms zero.
- Preview: 301 frames published at 9.614 Hz; an external client fetched a valid 640x360 JPEG snapshot of 29,205 bytes.
- Strict manifest/hash/index validation: `PASS`, no warnings.
- Full host decode: RGB 904, left IR 904, right IR 904, exactly matching indexes.
- Output size on board: 51 MiB.
- `rgb-gstreamer.log` contains normal EOS shutdown and no `handling interrupt`, demonstrating that Ctrl+C reached the Python owner rather than terminating the encoder directly.
- Post-stop port 8765 listener and `rsusb_capture`/`gst-launch-1.0`/`zstd` processes: none.
- Kernel evidence contains only D405 UVC discovery lines; no fatal USB/storage pattern.

## Verification deviation

The first board-side decode request stopped before examining video because board Python lacks optional `cv2`. No package was installed. The dependency-free board validator then passed, and the immutable 51 MiB session was copied to the workstation, where the same validator completed RGB and dual-IR full-frame decoding successfully.

## Operator repeat

The operator independently repeated the production command and stopped it with one terminal Ctrl+C. Session `rk3576-rsusb-20260904T065204Z-264f6aa4` sealed normally after 43.163621656 seconds. Board validation and workstation full decoding passed with exactly 1,295 RGB, 1,295 left-IR, 1,295 right-IR frames and 17,267 STM32 packets. Camera rates were 30.001966–30.001981 Hz and STM32 was 400.013521 Hz; continuity, timestamp, payload, CRC, validity, and queue errors were all zero. RGB shut down through normal EOS, the kernel log contained only one D405 UVC discovery line, and no capture/encoder process or preview listener remained.

## Rollback

Rollback is not required by this acceptance result. If later interactive-stop regression is observed, restore the preserved file only after operator approval, then verify SHA-256 and rerun this same bounded test.
