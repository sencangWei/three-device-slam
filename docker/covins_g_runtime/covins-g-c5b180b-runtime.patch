diff --git a/covins_backend/src/covins_backend/communicator_be.cpp b/covins_backend/src/covins_backend/communicator_be.cpp
index 1336356..7fe8d74 100644
--- a/covins_backend/src/covins_backend/communicator_be.cpp
+++ b/covins_backend/src/covins_backend/communicator_be.cpp
@@ -243,7 +243,9 @@ auto Communicator::Run()->void {
             this->ProcessAdditional();
             this->UnLock();
         }
-        vis_->DrawMap(map_);
+        if(vis_) {
+            vis_->DrawMap(map_);
+        }
 
         mapmanager_->ReturnMap(client_id_,check_num_map);
         map_ = nullptr; //make sure the MapManager is used correctly - this will cause SEGFAULT if accessed
diff --git a/covins_frontend/src/frontend_wrapper.cpp b/covins_frontend/src/frontend_wrapper.cpp
index 370848c..766af2d 100644
--- a/covins_frontend/src/frontend_wrapper.cpp
+++ b/covins_frontend/src/frontend_wrapper.cpp
@@ -499,9 +499,10 @@ bool FrontendWrapper::ParseCamParamFile(cv::FileStorage &fSettings)
     std::cout << "Left camera to Imu Transform (Tbc): " << std::endl << Tbc << std::endl;
 
     cv::cv2eigen(Tbc, Tsc_);
-    prev_pos_ = Tsc_.block<3, 1>(3, 0);
-    Eigen::Quaterniond temp_quat(Tsc_.block<3,3>(0,0));
-    prev_quat_ = temp_quat;
+    // Odometry is expressed relative to its own world origin.  Tsc_ is an
+    // extrinsic and must not be used as the previous odometry pose.
+    prev_pos_ = Eigen::Vector3d::Zero();
+    prev_quat_ = Eigen::Quaterniond::Identity();
     return true;
 }
 
